#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;

    int weight[10003], value[10003];

    for(int i = 1; i <= n; i++)
    {
        cin >> weight[i] >> value[i];
    }

    return 0;
}
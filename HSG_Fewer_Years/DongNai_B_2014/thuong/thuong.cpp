#include<bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);

    int n, m;
    cin >> n >> m;

    int arr[10003];
    for(int i = 1; i <= n; i++)
    {
        cin >> arr[i];
    }

    

    return 0;
}
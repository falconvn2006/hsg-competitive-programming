typedef long long ll;

#include<bits/stdc++.h>

using namespace std;

const char* INPUT = "build.inp";
const char* OUTPUT = "build.out";

int main()
{

    freopen(INPUT, "r", stdin);
    freopen(OUTPUT, "w", stdout);

    cin.tie(0);
    cin.sync_with_stdio();

    // Code here

    int n, m;
    cin >> n >> m;

    int arr[100004];

    while(m--)
    {
        int u, v;
        cin >> u >> v;

        
    }

    return 0;
}
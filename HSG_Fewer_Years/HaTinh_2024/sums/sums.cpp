typedef long long ll;

#include<bits/stdc++.h>

using namespace std;

const char* INPUT = "sums.inp";
const char* OUTPUT = "sums.out";

int main()
{

    freopen(INPUT, "r", stdin);
    freopen(OUTPUT, "w", stdout);

    cin.tie(0);
    cin.sync_with_stdio();

    // Code here
    int n, s;
    cin >> n >> s;

    int arr[100004];

    for(int i = 1; i <= n; i++)
    {
        cin >> arr[i];
    }

    

    return 0;
}